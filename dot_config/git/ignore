# Global gitignore. Git reads this path by default (XDG) when
# core.excludesFile is not set. Keep this to OS/editor junk only —
# project-specific ignores belong in each repo's .gitignore.
.DS_Store
._*
Thumbs.db

# mise machine-local tool pins (never belongs in a shared repo)
mise.local.toml
.mise.local.toml
