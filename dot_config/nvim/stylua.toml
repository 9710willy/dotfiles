# Pin the style explicitly so a stylua default change can never
# reformat the whole tree. These match the current defaults.
column_width = 120
line_endings = "Unix"
indent_type = "Tabs"
indent_width = 4
quote_style = "AutoPreferDouble"
call_parentheses = "Always"
