{{- if .isWorkMachine }}
[user]
	name = {{ .workGitName }}
	email = {{ .workGitEmail }}
	signingkey = {{ .workGitSigningKey }}
[commit]
	gpgsign = true
[gpg]
	program = gpg
{{- else }}
[user]
	name = 9710willy
	email = 9710willy@users.noreply.github.com
[commit]
	gpgsign = false
{{- end }}
